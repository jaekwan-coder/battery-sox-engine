# tests — 단위 테스트

`src/`의 각 모듈에 대응하는 테스트를 작성합니다. pytest 기준.

## 명명 규칙

`test_{모듈명}.py`. 예: `test_ecm.py`, `test_ekf.py`

## 우선순위

1. ECM의 알려진 해석해(1RC 계단응답)와 시뮬레이션 결과 비교
2. RLS/EKF의 합성 데이터(노이즈 없는 이상적 데이터)에서 정확한 수렴 확인
3. NIS 검정 함수 자체의 정확성 (알려진 카이제곱 분포로 검증)

## 실행

```bash
pytest tests/ -v
```
