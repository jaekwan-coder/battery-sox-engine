# 학습 로그

주차별로 무엇을 배우고, 무엇에서 막혔고, 어떻게 풀었는지 기록합니다.
**막힌 지점과 실패를 의도적으로 남깁니다.** 성공만 적힌 로그는 증거로서 힘이 없습니다.

각 항목은 아래 다섯 칸을 채우는 걸 원칙으로 합니다.
목표 / 실제로 한 것 / 막힌 지점 / 어떻게 풀었나 (또는 못 풀었나) / 다음 액션

---

## W0 — 기초 정비 (2026-09-05)

### 목표
회로·제어·확률 이론 중 SOX 구현에 실제로 쓰이는 최소 세트를 복원한다.
자가진단 8문항(RC 지수응답, 라플라스/극점, 상태공간, 가관측성, 공분산,
가우시안 곱셈, 최소자승법, 이산화)에 스스로 답할 수 있는 상태를 목표로 함.

### 실제로 한 것
- 8문항 자가진단 실시 → 8개 중 6개 막힘 확인
- RC 병렬 임피던스를 ω 0→∞로 손계산 후 나이퀴스트 평면에 직접 플롯
  → 반원이 나오는 것을 확인 (`notebooks/00_rc_nyquist.ipynb`)
- 전진 오일러 vs ZOH 이산화 비교, Δt/τ 비율을 바꿔가며 안정성 관찰
  (`notebooks/01_discretization.ipynb`)

### 막힌 지점
- "가관측성"이라는 말을 처음엔 그냥 "센서가 있냐 없냐"로 오해함.
  OCV 곡선 기울기와의 연결을 못 잡고 있었음.
- 가우시안을 "곱한다"는 게 왜 "가중합"이 아닌지 직관이 안 잡힘.
  확률의 AND/OR 구분을 다시 봐야 했음.

### 어떻게 풀었나
- 가관측성: "출력(전압) = 상태(SOC)의 함수인데, 그 함수의 기울기가 0이면
  상태가 변해도 출력에 안 나타난다"로 재정의하고 나서야 LFP 평탄 구간
  문제와 연결됨.
- 가우시안 곱: 주사위 두 개 동시에 6 나올 확률 예제로 AND=곱셈이라는
  걸 먼저 확인한 뒤, "모델 예측도 맞고 측정도 맞다고 믿는다"는 우리
  상황이 AND라는 걸 납득함.

### 다음 액션
- W1 진입 전 EIS 나이퀴스트 ↔ RC 회로 대응을 한 번 더 손으로 확인
- OCV(SOC) 곡선의 실제 데이터(LG HG2)를 아직 안 봤음 → W1에서 확인

---

## W1-2 — 셀→회로 번역 (2026-09-05)

### 목표
전기화학 최소 세트(산화환원, 화학포텐셜, OCV-SOC 곡선의 소재별 형태,
히스테리시스, 3단계 율속)를 이해하고 M1.6에서 2RC ECM을 상태공간으로
조립한다. 이어서 EIS·나이퀴스트 선도를 유도해 시간영역(M1)과
주파수영역(M2)이 같은 물리를 어떻게 다르게 보여주는지 확인한다.

### 실제로 한 것
- M1.0~M1.6: 전압의 정의부터 화학포텐셜, OCV-SOC 곡선(고용체 vs
  상전이), 히스테리시스, R0/RC1/RC2의 물리적 기원(3단계 율속)까지
  순서대로 정리 (`docs/02_theory/W1_electrochemistry_ecm.md`)
- M2.0~M2.7: 교류·임피던스 기초부터 RC 병렬의 반원 유도(W0 Q1/Q2
  재활용), RC 여러 개 직렬연결 시 반원이 여러 개로 분리되는 이유를
  숫자로 직접 계산, Randles 회로의 위치(방향 A/B 구분), Warburg
  임피던스(45도선), 나이퀴스트 vs 보데 선도, EIS의 실무 활용까지 정리
  (`docs/02_theory/W2_eis.md`)

### 막힌 지점
- M2.2에서 "RC 여러 개가 직렬로 연결되면 왜 반원이 여러 개인가"를
  처음엔 "그냥 반원이 여러 개 생긴다" 정도로만 이해하고 넘어가려 했음.
  실제로 왜 그런지 수식으로 확인이 안 된 상태였음.
- Randles 회로가 왜 갑자기 튀어나온 개념처럼 느껴졌음. "나이퀴스트
  선도를 회로로 정리한다"는 말의 논리 방향(회로→곡선 vs 곡선→회로)이
  헷갈렸음.
- "노화"와 "열화"를 같은 말로 무심코 섞어 썼는데, 정확히 같은 뜻인지
  헷갈림.

### 어떻게 풀었나
- 반원 분리: τ1=2초, τ2=100초인 두 RC 블록을 실제 숫자(ω=10, 0.5,
  0.01 rad/s)로 각각 계산해보니, τ가 충분히 다르면 "블록1이 반원을
  다 그리고 끝난 뒤에야 블록2가 시작"하는 게 직접 확인됨. τ가
  가까우면(2초 vs 3초) 이게 안 되고 뭉개진다는 것도 확인 — W0 Q2의
  "라플라스 극점이 가까우면 구분 안 됨"과 같은 문제였음.
- Randles 회로: "방향 A(회로→곡선, 우리가 M2.1에서 한 것)"와
  "방향 B(곡선→회로, 실제 실험에서 하는 것)"를 구분하고 나니, Randles
  회로가 "방향 B를 위한 표준 템플릿"이라는 게 납득됨. 새 이론이
  아니라 M1.6에서 이미 만든 회로에 이름표가 붙은 것.
- 노화/열화: 노화=현상(결과), 열화=메커니즘(원인)이라는 위계 관계로
  정리. "노화됐다 → 어느 열화 메커니즘 때문인가?"가 EIS 진단의 정확한
  구조였음.

### 다음 액션
- 구현 과제 A: 2RC+히스테리시스 ECM 시뮬레이터 (`src/models/ecm.py`)
- 구현 과제 B: 나이퀴스트 선도 시뮬레이션, τ1/τ2 반원 분리·뭉개짐을
  실제 코드로 재현 (`notebooks/00_rc_nyquist.ipynb`)
- W3 진입 전 이 두 과제를 완료해 "이해했다"를 "구현할 수 있다"로
  전환할 것

---

## W3 — 파라미터 식별 (2026-09-06)

### 목표
W1-2에서 만든 ECM 구조에 들어갈 실제 숫자(OCV 곡선, R0/R1/C1)를
공개 실험 데이터에서 뽑아내는 방법을 이해한다. 아울러 노화로 파라미터가
변할 때 이를 실시간으로 갱신하는 RLS까지 다룬다.

### 실제로 한 것
- M3.0~M3.5 이론 정리 (`docs/02_theory/W3_parameter_identification.md`)
- LG HG2 / Samsung 30T 공개 데이터셋의 실제 구조를 Mendeley 원문에서
  확인하고 `data/README.md`에 정리
- 시험 블록별 목적(HPPC / C20 / 드라이브사이클)과 커리큘럼 대응 매핑

### 막힌 지점
- "SOC를 추정하는 게 목적인데, HPPC 실험은 어떻게 시작부터 SOC를
  90%로 정확히 맞춘다는 것인가?" — 순환논리처럼 보여서 한참 걸렸음.
- 공개 데이터셋이 셀 제조사(LG)가 직접 낸 것인지, 제3자가 낸 것인지
  구분하지 못하고 있었음.
- **가장 중요한 발견**: 초반에 "LG HG2 데이터셋의 HPPC를 쓰자"고
  계획했는데, Mendeley 공개 문서를 실제로 확인해보니 **LG HG2에는
  HPPC가 명시적으로 나열되어 있지 않았음.** 드라이브 사이클이 중심.
  확인 없이 가정하고 계획을 세웠던 것.

### 어떻게 풀었나
- SOC 참값 문제: 실험실과 차량은 완전히 다른 조건이라는 걸 깨달음.
  완충=CC-CV 종료 시점으로 **정의**하고, 총 용량 Q를 직접 재고,
  정밀 장비(풀스케일 0.1% 정확도)로 짧은 시간만 적산하면 쿨롱카운팅이
  사실상 정확함. 차량의 문제(값싼 센서 + 수년 누적)가 여기선 안 생김.
  → 순환이 아니라 위계 구조(실험실 참값 → 모델 → 차량이 모델 참조).
- 데이터 출처: "LG"는 셀 모델명일 뿐이고, 시험·공개 주체는 McMaster
  University 연구실. 제조사 내부 데이터는 영업비밀이라 공개 안 됨.
- HPPC 문제: 같은 실험실(Kollmeyer)의 **Samsung INR21700 30T**
  데이터셋에는 HPPC 프로토콜이 명확히 문서화되어 있음을 확인.
  → 두 데이터셋을 함께 쓰는 전략으로 전환 (Samsung 30T로 파라미터
  식별, LG HG2 드라이브 사이클로 필터 검증).

### 배운 것 (방법론)
**데이터셋을 계획에 넣기 전에 반드시 공식 문서를 직접 확인할 것.**
이름만 보고 "당연히 있겠지" 하고 가정하면 나중에 계획 전체를 다시
짜야 한다. 이번엔 코드 작성 전에 발견해서 손실이 적었음.

### 다음 액션
- 과제 C: HPPC 파라미터 추출 — **합성 데이터(정답을 아는 가짜 HPPC)로
  피팅 코드를 먼저 검증**한 뒤 실데이터 적용. 코드 오류와 데이터 해석
  오류를 분리하기 위함.
- 과제 D: RLS 온라인 식별 + PE 조건 위반 재현

---

## 구현 과제 A, B — ECM 시뮬레이터 + 나이퀴스트 노트북 (2026-09-06)

### 목표
W1.6에서 유도한 2RC+히스테리시스 ECM을 실제 동작하는 코드로 만들고,
W0(계단응답, 이산화)와 W2(나이퀴스트 반원)에서 손으로 계산했던 결과와
일치하는지 검증한다.

### 실제로 한 것
- `src/models/ecm.py`: ECM 클래스, ZOH 이산화, 히스테리시스 모델 구현
- `tests/test_ecm.py`: 6개 단위테스트 작성
  - R0 즉시강하, RC 계단응답(해석해와 비교), 63.2% 규칙(W0 Q1),
    쿨롱카운팅, HPPC 펄스 구조, 히스테리시스 수렴
- `notebooks/00_rc_nyquist.ipynb`: RC 반원 유도(W2.1), 두 블록 직렬연결
  시 반원 분리/뭉개짐(W2.2), 나이퀴스트-보데 비교(W2.6)를 코드로 재현

### 막힌 지점 (그대로 기록)
- 히스테리시스 모델의 부호를 처음에 반대로 짰음. 방전(전류 양수)일 때
  `target = +h_max`로 짜서 테스트가 실패함. W1.4에서 "충전 곡선이 위,
  방전 곡선이 아래"라고 설명해놓고 정작 코드에서는 반대 부호를 씀.
- 히스테리시스 수렴 테스트에서 시간을 2000초만 줬는데, 실제 수렴
  시상수가 그보다 커서(Q_As/(k|I|) 계산 시 약 2160초) 63%만 수렴한
  상태였음. "5tau는 지나야 99% 수렴"이라는 W0 Q1의 규칙을 테스트
  설계에는 깜빡하고 안 적용했던 것.
- 노트북 그래프에 한글 라벨을 넣었더니 폰트가 없어 깨짐(□□□ 표시).
  그래프 텍스트는 영문으로, 설명은 한글 마크다운으로 분리해야 한다는
  것을 이번에 알게 됨.

### 어떻게 풀었나
- 부호: `target = -h_max * sign(I)`로 수정 (방전=양수 전류 규약과
  일치시킴)
- 시간: 테스트 안에서 수렴 시상수를 직접 계산해 `duration = 6*tau`로
  동적으로 설정하도록 수정. 하드코딩된 시간 대신 물리에서 필요한
  시간을 역산하는 방식으로 바꿈.
- 폰트: 노트북의 `matplotlib` 라벨/타이틀을 전부 영문으로, 설명 문단만
  한글 마크다운 셀로 분리

### 검증 결과
- 6개 테스트 전부 통과 (RC1 계단응답이 해석해와 오차 4e-16 수준으로 일치)
- 나이퀴스트 반원 지름·꼭대기 위치가 이론값과 일치 (오차 <2%)
- tau 비율 50배 → 반원 분리, 1.5배 → 반원 뭉개짐을 시각적으로 확인

### 다음 액션
- 과제 C: HPPC 파라미터 추출 (`src/identification/hppc.py`)
  - 이 ECM으로 합성 HPPC 데이터를 만들고(`make_hppc_pulse_current`
    함수 이미 준비됨), 그 데이터에서 다시 R0/R1/C1을 피팅해 원래
    값과 일치하는지 검증하는 순서로 진행

---

## README 정비 — 전체 구조 한눈에 보기 (2026-09-07)

### 목표
새 코드(`ecm.py` 등)를 받았을 때 코드 문법을 몰라도 "이 저장소에
뭐가 어디 있는지" 구조부터 파악할 수 있도록, README에 전체 파일
트리와 각 파일의 역할을 정리한다.

### 배운 것 (코드 읽는 법)
코드를 한 줄씩 다 읽는 대신, 구조부터 보는 습관을 들이기로 함:
1. 파일 안에 class가 몇 개인지 (몇 개의 "물건"이 정의됐는지)
2. 각 class 안에 def가 몇 개인지 (그 물건이 뭘 할 수 있는지)
3. 중요한 함수 하나의 입력·출력(시그니처)만 먼저 확인
4. 계산 본문은 필요할 때만 열기

이게 소프트웨어공학에서 "하향식(top-down) 읽기"라고 부르는 표준
접근법과 같다는 것을 확인함. `ecm.py`의 `output_voltage` 함수 마지막
줄이 `W1_electrochemistry_ecm.md`의 단자전압 공식과 그대로 대응된다는
것도 이번에 명시적으로 확인 — 코드가 새로운 게 아니라 이론을 그대로
옮긴 것이라는 감각을 잡음.

### 실제로 한 것
- README.md의 "저장소 구조" 섹션을 실제 파일 전체(✅완료/🔲예정 표시
  포함)를 반영한 트리로 전면 교체
- "코드를 처음 보실 때" 섹션 추가 (위 4단계 체크리스트)
- 트리에 언급된 모든 파일명을 실제 `find` 결과와 대조해 불일치 없음을 확인

---

## 로그 작성 규칙

- 매주 최소 1회, 이상적으로는 막히는 지점이 생길 때마다 추가
- "막힌 지점"은 부끄러워하지 않고 구체적으로 적기 (모호하게 "어려웠다"가 아니라
  정확히 어느 개념에서 어떤 오해를 했는지)
- 그림/코드 결과를 참조할 때는 상대경로로 링크
- 이 문서는 시간순 기록이고, 재사용 가능한 개념 설명은 `02_theory/`로 옮길 것


---

## hppc.py 구현 — HPPC 파라미터 추출 (2026-09-07)

### 목표
ecm.py의 역과정으로, 전압·전류 데이터에서 R0, R1, C1을 뽑아내는
코드(hppc.py)를 만들고, 합성 데이터로 검증한다.

### 실제로 한 것
- `src/identification/hppc.py` 작성: find_pulse_edges, extract_r0,
  fit_relaxation, extract_hppc_parameters, validate_with_synthetic_data
- ecm.py로 R0=0.02, R1=0.01, C1=100인 가짜 HPPC 데이터를 생성한 뒤,
  같은 값을 hppc.py로 역산해 비교하는 자체 검증 실행

### 검증 결과
전부 통과, 상대오차 0.00% 수준으로 정확히 일치.

PASS: R0 참값=0.02, 추출값=0.02, 상대오차=0.00%
PASS: R1 참값=0.01, 추출값=0.01, 상대오차=0.00%
PASS: C1 참값=100, 추출값=100, 상대오차=0.00%
완화곡선 피팅 RMSE = 7.47036e-11 V


### 의미
ecm.py(순방향: 파라미터→전압)와 hppc.py(역방향: 전압→파라미터)가
서로 정확히 짝을 이룬다는 것을 확인함. 이는 "합성 데이터로 코드를
먼저 검증한다"는 원칙(W3_parameter_identification.md)이 실제로
성립함을 보여줌.

### 현재 범위와 한계
1RC만 구현했고, 2RC(R2, C2)는 제외됨(W3_hppc_code.md 설계 결정 참고).
아직 실제 데이터(Samsung 30T)는 사용하지 않은 상태 — 합성 데이터
검증만 완료.

### 다음 액션
- 나쁜 초기값을 줬을 때 피팅이 실패하는 양상 재현 (W3_hppc_code.md
  검증계획 3번)
- Samsung INR21700 30T 실제 HPPC 데이터 다운로드 및 적용